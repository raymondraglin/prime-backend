# PRIME Backend Environment Variables

# Required
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
OPENAI_API_KEY=sk-your-openai-api-key-here
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/control_center

# Optional — Model Selection
PRIME_MODEL=gpt-4o
# PRIME_MODEL=gpt-5.2  # More expensive but higher quality

# Optional — Task Queue (Celery + Redis)
REDIS_URL=redis://localhost:6379/0
# If using Memurai on Windows, default localhost:6379 works fine
# If using Railway/Render, this gets auto-set by the Redis addon

# Optional — Web Search
BRAVE_API_KEY=your-brave-search-api-key
# TAVILY_API_KEY=your-tavily-api-key

# Optional — GitHub Integration
GITHUB_TOKEN=ghp_your-github-personal-access-token
# Needed for: push_github_file, create_pull_request, search_github_code
# Scopes: repo (full control of private repositories)

# Optional — Deployment
ENVIRONMENT=development
# production | staging | development
